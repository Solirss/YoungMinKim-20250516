# 올웨이즈 AI 가성비 점수

> **레브잇 Problem Solver Internship (AI Agent) — 과제 2**
>
> 과제 1에서 설계한 MVP("초개인화 나만의 가성비 점수")를 실제로 구현한 서비스입니다.
> 유저의 클릭 행동을 신호로 수집해 성향(브랜드 가치파 / 실속 용량파 / 복합형)을 실시간 판별하고,
> GPT-4o-mini가 성향에 맞는 가성비 점수를 0~99점으로 산출해 상품 카드에 배지로 표시합니다.

---

## 🔗 배포 링크

| 서비스 | URL |
|---|---|
| 프론트엔드 (Vercel) | `-` |
| 백엔드 API (Render) | `-` |
---

## 목차

1. [서비스 개요](#서비스-개요)
2. [기술 스택](#기술-스택)
3. [프로젝트 구조](#프로젝트-구조)
4. [핵심 구현 상세](#핵심-구현-상세)
   - [다나와 크롤링](#1-다나와-크롤링)
   - [성향 판별 알고리즘](#2-성향-판별-알고리즘)
   - [GPT 가성비 점수 산출](#3-gpt-가성비-점수-산출)
   - [Cold Start 처리](#4-cold-start-처리)
   - [GPT 실패 시 로컬 폴백](#5-gpt-실패-시-로컬-폴백)
5. [API 명세](#api-명세)
6. [환경변수](#환경변수)
7. [설계 의도 및 강조 포인트](#설계-의도-및-강조-포인트)
8. [아쉬운 점 / 개선 여지](#아쉬운-점--개선-여지)
9. [설계 고민 요소](#설계-고민-요소-미해결-과제)

---

## 서비스 개요

### 배경 (과제 1 요약)

고물가 시대에 소비자는 단돈 몇천 원을 아끼기 위해 여러 앱을 넘나들며 시간을 낭비합니다.
올웨이즈 앱의 핵심 문제는 **"이 가격이 진짜 싼 건가?"** 라는 확신 없이 구매를 결정해야 하는 인지 부담입니다.

기존 이커머스가 "텍스트 설명"과 "별점"으로 가성비를 안내한다면,
올웨이즈는 **숫자 배지 하나**로 0.1초 안에 구매 확신을 줍니다.

### 핵심 차별점

| 항목 | 기존 이커머스 | 올웨이즈 AI |
|---|---|---|
| 제공 형태 | 장황한 텍스트 설명 | ⚡ 가성비 00점 배지 |
| 유저 경험 | 읽고 해석하는 노동 | 🎯 0.1초 인지 제로 |
| 개인화 | 모두에게 동일한 추천 | ✅ 나의 쇼핑 패턴 기반 |
| 심리적 효과 | "정말 싼 게 맞나?" | "나한테 최고다" 확신 |

### 사용 흐름

```
1. 유저가 카테고리 클릭 또는 직접 검색
        ↓
2. 다나와에서 상품 크롤링 (실패 시 목 데이터 폴백)
        ↓
3. 유저의 클릭 행동을 분류해 성향 판별
        ↓
4. GPT-4o-mini가 성향에 맞는 기준으로 가성비 점수(0~99) 산출
        ↓
5. 상품 카드에 점수 배지 + 툴팁 표시
```

---

## 기술 스택

| 영역 | 기술 | 선택 이유 |
|---|---|---|
| 프론트엔드 | React 18 (JavaScript, CRA) | 과제 제약 조건 |
| 백엔드 | Node.js 20 + Express 4 | 과제 제약 조건 |
| AI | OpenAI GPT-4o-mini | 과제 제약 조건 (ChatGPT API 활용) |
| 크롤링 | axios + cheerio | 다나와 정적 HTML 파싱에 최적 |
| 프론트 배포 | Vercel | React 앱 무료 호스팅 |
| 백엔드 배포 | Render | Node.js 무료 호스팅, 환경변수 지원 |

---

## 프로젝트 구조

```
always-app/
├── render.yaml                      # Render 배포 설정
├── README.md
│
├── backend/
│   ├── package.json
│   ├── .env.example                 # 환경변수 템플릿
│   └── src/
│       ├── index.js                 # Express 서버 진입점, API 엔드포인트 정의
│       ├── crawler.js               # 다나와 크롤링 + 목 데이터 폴백 + 시장 기준선 계산
│       └── scorer.js                # 성향 판별 알고리즘 + GPT 점수 산출 + 로컬 폴백
│
└── frontend/
    ├── package.json
    ├── vercel.json                  # Vercel 배포 설정 (SPA 라우팅)
    └── src/
        ├── App.js                   # 메인 컴포넌트: 상태 관리, 행동 로그 수집, API 호출
        ├── App.css                  # 전체 스타일
        ├── index.js                 # React 앱 엔트리포인트
        └── components/
            ├── ProductList.js       # 상품 카드 그리드 + ScoreBadge + PriceChart
            ├── Cart.js              # 장바구니 슬라이드 드로어
            ├── PersonaBadge.js      # 헤더 성향 칩 (실시간 업데이트)
            ├── SearchBar.js         # 검색창 (controlled component)
            └── OnboardingBanner.js  # 초기 진입 안내 화면 + 비교표
```

---

## 핵심 구현 상세

### 1. 다나와 크롤링

**다나와를 선택한 이유:**
- 정적 HTML 렌더링 구조라 `axios + cheerio` 크롤링이 안정적
- 쿠팡(SPA 구조), 네이버 쇼핑(JS 렌더링) 모두 정적 크롤링이 불안정함을 확인 후 전환
- 여러 쇼핑몰 최저가를 한 번에 비교 제공해 시장 평균 단가 계산에 유리
- 별도 API 키 불필요

**크롤링 항목:**

| 필드 | 다나와 선택자 | 비고 |
|---|---|---|
| `name` | `.prod_name a` | 상품명 |
| `price` | `.price_sect a strong` | 최저가, 숫자만 추출 |
| `image` | `.thumb_wrap img[src]` | 프로토콜-상대 URL 보정 |
| `brand` | `.prod_name .prod_eng_name` | 없는 경우 빈 문자열 |
| `rating` | `.star_rating[data-rating]` | JS 로드 구조라 수집 불가 → 0 |
| `reviews` | `.cnt_opinion` | JS 로드 구조라 수집 불가 → 0 |
| `volume` | 상품명 정규식 파싱 | `(\d+)\s*(kg\|g\|ml\|L\|매\|롤...)` |

> **다나와 제약**: 평점과 리뷰 수는 별도 API로 동적 로드되어 정적 크롤링으로 수집 불가.
> 이로 인해 성향 판별과 GPT 점수 산출에서 해당 신호를 제외했습니다.

**크롤링 실패 시 폴백:**

```
크롤링 결과 3개 미만
    → resolveMockCategory() 로 키워드→카테고리 매핑
    → MOCK_DATA[category] 목 데이터 반환
```

**크롤링 후 시장 기준선 계산:**

```js
// 같은 검색 결과 내 상품들의 평균을 시장 평균으로 정의
avgPrice = 전체 상품 평균가
avgPricePerUnit = pricePerUnit이 있는 상품들의 평균 단가

// 이를 기반으로 각 상품에 플래그 추가
isPremium = price > avgPrice * 1.3   // 시장 평균의 130% 이상
```

---

### 2. 성향 판별 알고리즘

유저가 상품을 클릭할 때마다 5가지 신호를 실시간으로 수집합니다.
같은 상품의 같은 신호는 `"productId:signalType"` 키로 중복을 방지합니다 (예: `"danawa-0-123:bulk"`).

**신호 가중치 체계:**

```
[브랜드파 신호]
  +2  동일 브랜드 반복 클릭 (같은 브랜드 상품을 2회 이상 클릭한 브랜드 수)
  +2  프리미엄 상품 클릭   (시장 평균가 130% 이상인 상품 클릭)
  +1  평균가 이상 상품 클릭 (다나와 상품에 brand 필드가 없는 경우 보완 신호)

[용량파 신호]
  +1  대용량 상품 클릭     (isBulk 상품. 가중치 낮은 이유: 다나와 대부분 대용량)
  +2  저단가 상품 클릭     (시장 평균 단가의 80% 이하 상품)
```

**성향 판별:**

```
brandRatio = brandScore / (brandScore + volumeScore)

brandRatio >= 0.6  →  "brand"   (브랜드 가치파)
brandRatio <= 0.4  →  "volume"  (실속 용량파)
그 외              →  "mixed"   (복합 분석)
총합 < 3           →  "unknown" (Cold Start, 점수 미노출)
```

**프론트엔드에서도 동일 로직 실행:**
백엔드의 `detectPersonaType()`과 동일한 알고리즘을 `App.js`의 `calcPersona()`로 프론트에도 구현해,
헤더의 `PersonaBadge`가 API 응답 없이 클릭 즉시 실시간으로 업데이트됩니다.
---

### 3. GPT 가성비 점수 산출

성향별로 완전히 다른 평가 기준 프롬프트를 구성해 GPT-4o-mini에 전달합니다.

**성향별 평가 기준:**

| 성향 | 핵심 기준 | 보조 기준 |
|---|---|---|
| brand | 가격 타이밍: 현재가 vs 3달 최저가 | isPremium 여부, 선호 브랜드 일치 여부 |
| volume | 단위당 단가 vs 시장 평균 | isBulk 여부 (대용량 가점) |
| mixed | 가격 타이밍(30%) + 단위 단가(30%) | 브랜드 신뢰도(20%) + 종합 가성비(20%) |

**GPT에 전달하는 상품 데이터 (토큰 절약을 위해 핵심만):**

```json
{
  "index": 0,
  "name": "피죤 울트라 세탁세제 3kg",
  "brand": "피죤",
  "price": 12900,
  "pricePerUnit": 4,
  "unit": "g",
  "minPrice3M": 11500,
  "maxPrice3M": 14200,
  "marketAvgPrice": 14833,
  "marketAvgPricePerUnit": 5,
  "isPremium": false,
  "isBulk": true
}
```

**프롬프트 제약 조건 (점수 쏠림 방지):**

```
- 0~99점 범위 사용
- 상품 간 점수 차이 최소 10점 이상 (전부 90점대 절대 금지)
- 진짜 가성비 좋은 상품 1~2개만 80점 이상
- 가격이 3달 최고가에 가까운 상품 → 35점 이하 가능
- 시장 평균보다 단가 비싼 상품 → 45점 이하
```

**GPT 응답 형식:**

```json
{"scores":[{"index":0,"score":74,"tooltip":"최저가 대비 3% · 리뷰 1.2만개"}]}
```

- `temperature: 0.3` — 동일 입력에 점수가 크게 달라지지 않도록 낮게 설정
- `max_tokens: 900` — 상품 10개 기준 여유 있는 상한

---

### 4. Cold Start 처리

> **"엉성한 점수로 첫인상을 망치는 것보다 숨기는 편이 낫다"**

가중치 합계 3점 미만 시:
- 상품 카드의 점수 배지 → `🔒 점수 대기중` 표시
- 헤더 PersonaBadge → `성향 분석 중 (N/3)` 표시
- GPT 점수 요청 자체를 하지 않음 (`scoreSource: "none"`)

---

### 5. GPT 실패 시 로컬 폴백

GPT API 오류 발생 시 `calcLocalScore()`로 자동 전환합니다.

**브랜드파 / 복합형 (가격 타이밍 기반):**

```
trustScore  = log10(reviews+1) / log10(30000) × 15  → 0~15점
            + rating >= 4.7 ? 5점 : rating >= 4.0 ? 2점 : 0점  → 최대 5점
            = 최대 20점

position    = (현재가 - 3달최저가) / (3달최고가 - 3달최저가)  → 0=최저가, 1=최고가
timingScore = (1 - position) × 79  → 0~79점

최종 점수 = min(99, timingScore + trustScore)
```

**용량파 (단가 절약률 기반):**

```
ratio      = 시장평균단가 / 이상품단가  → 1=평균, 2=절반가격
valueScore = min(ratio × 55, 79)  → 0~79점

최종 점수 = min(99, valueScore + trustScore)
```

`scoreSource` 필드로 점수 출처를 명시해 디버깅을 지원합니다 (`"gpt"` / `"local"` / `"none"`).

---

## API 명세

### `GET /health`

배포 플랫폼 헬스체크용.

**Response:**
```json
{ "status": "ok" }
```

---

### `POST /api/products`

상품 검색, 성향 판별, 가성비 점수 산출을 수행합니다.

**Request Body:**

```json
{
  "keyword": "세제",
  "category": "세제",
  "userLog": {
    "clickedBrands": ["피죤", "아리엘"],
    "brandClickCounts": { "피죤": 2, "아리엘": 1 },
    "premiumClicks": 1,
    "avgAboveClicks": 0,
    "bulkClicks": 2,
    "lowUnitPriceClicks": 0,
    "totalClicks": 5
  }
}
```

| 파라미터 | 타입 | 필수 | 설명 |
|---|---|---|---|
| `keyword` | string | ✅ | 다나와 검색 키워드 |
| `category` | string | | UI 카테고리 레이블 (목 데이터 매핑에 사용) |
| `userLog` | object | | 클라이언트 누적 행동 로그. 없으면 `{}` 처리 |

**Response:**

```json
{
  "personaType": "brand",
  "scoreSource": "gpt",
  "totalCount": 6,
  "products": [
    {
      "id": "danawa-0-1718000000000",
      "name": "피죤 울트라 세탁세제 3kg",
      "price": 12900,
      "brand": "피죤",
      "image": "https://img.danawa.com/...",
      "rating": 0,
      "reviews": 0,
      "volume": { "amount": 3000, "unit": "g" },
      "pricePerUnit": 4,
      "isPremium": false,
      "isBulk": true,
      "source": "danawa",
      "marketAvgPrice": 14833,
      "marketAvgPricePerUnit": 5,
      "priceHistory": [
        { "date": "2024-10-01", "price": 13200 },
        { "date": "2025-01-01", "price": 12900 }
      ],
      "valueScore": 78,
      "scoreTooltip": "최저가 대비 2% · 대용량 할인"
    }
  ]
}
```

| 필드 | 설명 |
|---|---|
| `personaType` | `"brand"` / `"volume"` / `"mixed"` / `"unknown"` |
| `scoreSource` | `"gpt"` (정상) / `"local"` (GPT 실패 폴백) / `"none"` (Cold Start) |
| `valueScore` | 0~99점. `null`이면 Cold Start 상태 |
| `scoreTooltip` | 25자 이내 점수 근거. `null`이면 미노출 |
| `source` | `"danawa"` (크롤링) / `"mock"` (목 데이터 폴백) |

**에러 응답:**

```json
{ "error": "keyword is required" }   // 400
{ "error": "서버 오류가 발생했습니다." }  // 500
```

---

### `GET /api/debug-crawl?keyword=세제`

개발 환경 전용. 크롤링 원본 데이터를 확인합니다.

**Response:**
```json
{ "count": 8, "products": [...] }
```

---

## 환경변수

### 백엔드 (`backend/.env`)

```env
OPENAI_API_KEY=sk-...        # OpenAI API 키 (필수)
PORT=4000                    # 서버 포트 (기본값: 4000)
FRONTEND_URL=https://...     # CORS 허용 프론트엔드 URL (미설정 시 전체 허용)
```

### 프론트엔드 (Vercel 환경변수 또는 `frontend/.env.local`)

```env
REACT_APP_API_URL=https://...   # 백엔드 API 기본 URL (미설정 시 package.json proxy 사용)
```

---

## 설계 의도 및 강조 포인트

### "AI가 쓴 추천 글을 유저가 또 읽게 하지 않는다"

GPT의 역할을 점수 계산기로만 한정하고, 유저에게는 **숫자 배지 + 25자 이내 툴팁**만 노출합니다.
GPT가 생성한 텍스트를 그대로 보여주는 방식은 기존 AI 추천과 차별화되지 않기 때문입니다.

### 카테고리 분리 없이 유저 자체를 분석

카테고리별로 성향을 따로 추적하지 않고, 전체 쇼핑 행동을 글로벌하게 누적합니다.
클릭이 쌓일수록 성향이 동적으로 업데이트되며, 브랜드파에서 용량파로 이동하는 변화도 반영됩니다.

### 알고리즘 vs GPT 역할 분리

카테고리 키워드 매핑 같은 단순 로직은 알고리즘으로, 맥락 이해가 필요한 점수 산출은 GPT로 처리합니다.
두 역할을 명확히 분리해야 GPT 비용을 최소화하면서 유연성을 유지할 수 있습니다.

### 프론트/백엔드 성향 판별 동기화

`App.js`의 `calcPersona()`와 `scorer.js`의 `detectPersonaType()`은 동일한 가중치 공식을 사용합니다.
프론트는 헤더 실시간 업데이트용, 백엔드는 GPT 프롬프트 맥락 주입용으로 역할이 구분됩니다.

---

## 아쉬운 점 / 개선 여지

1. **실시간 가격 히스토리**: 현재는 시뮬레이션 데이터. 실 서비스에서는 스케줄러로 주기적 크롤링 후 DB 적재 필요
2. **다나와 크롤링 안정성**: HTML 구조 변경 시 CSS 선택자가 깨질 수 있음. 지속적인 모니터링 필요
3. **평점/리뷰 수집 불가**: 다나와는 평점과 리뷰를 JS로 별도 로드해 정적 크롤링으로 수집 불가. 추후 다른 데이터 소스 보완 검토
4. **유저 로그 영속성**: 현재 클라이언트 메모리에만 존재해 새로고침 시 초기화됨. 실 서비스에서는 로그인 연동 + DB 저장 필요
5. **성향 타입 확장**: 현재 3가지(brand/volume/mixed). "시간 절약파", "친환경파" 등 다축 클러스터링 가능
6. **온보딩 성향 테스트**: Cold Start를 앞당기기 위해 신규 유저 대상 간단한 퀴즈("브랜드가 중요한가요, 용량이 중요한가요?") 도입 검토
7. **카테고리 5개 제한**: 현재 세제/휴지/기저귀/쌀/키친타올만 목 데이터 보유. 더 많은 카테고리 추가 필요

---

## 설계 고민 요소 (미해결 과제)

MVP를 만들면서 명확한 답을 내리지 못한 구조적 고민입니다.

### 1. GPT에 점수를 위임하는 게 맞는가, 자체 알고리즘이 맞는가

| 항목 | GPT | 자체 알고리즘 |
|---|---|---|
| 맥락 이해 | 상품명, 브랜드, 가격 범위를 종합 판단 가능 | 명시적으로 정의한 변수만 반영 |
| 일관성 | 동일 입력에도 결과가 달라질 수 있음 | 항상 동일한 결과 보장 |
| 비용/속도 | 요청당 API 비용 + 레이턴시 발생 | 무료, 즉시 계산 |
| 설명 가능성 | 프롬프트로 조정 가능하나 블랙박스 | 수식이 투명해 디버깅 용이 |
| 확장성 | 파라미터 추가 시 프롬프트 수정만으로 반영 가능 | 수식 재설계 필요 |

**결론**: 초기에는 GPT로 빠르게 검증하고, 유저 데이터가 쌓이면 자체 알고리즘 + ML로 전환하는 것이 맞다고 봅니다.
지금 단계에서 알고리즘을 정교하게 만들어봤자 실제 유저 데이터 없이는 가중치를 검증할 방법이 없기 때문입니다.

### 2. 가성비 점수에 반영되지 못한 파라미터들

현재 점수에 반영된 파라미터는 가격 타이밍, 단위 단가, 평점, 리뷰 수 4가지입니다.
실제 가성비에는 훨씬 많은 변수가 관여합니다:

- 계절/시기 요인 (명절 전후 가격 급등)
- 행사 주기 (쿠팡 로켓와우 특가, 네이버 장보기 할인 주기)
- 대체재 존재 여부 (유사 성분/스펙 상품과의 비교)
- 재구매율 / 반품율 (실제 만족도 지표)
- 유통기한/소비 주기 (쌀 20kg은 가성비가 좋아도 혼자 사는 사람에겐 의미 없음)

이를 수식으로 모두 정의하기보다 **실제 구매 전환 데이터를 레이블로 ML 모델을 학습**시키는 것이 현실적입니다.
다만 이는 상당한 양의 유저 행동 데이터가 쌓인 이후의 과제입니다.

### 3. 카테고리별 가성비 지수 vs 데이터 요구량 트레이드오프

기저귀의 가성비와 쌀의 가성비는 완전히 다른 기준으로 평가되어야 합니다.
기저귀는 피부 안전성과 브랜드 신뢰도가 핵심이고, 쌀은 원산지와 단위 단가가 핵심입니다.
카테고리별로 다른 가중치 체계를 적용하면 정확도가 크게 올라가지만, 카테고리 수만큼 학습 데이터와 모델 관리 비용도 늘어납니다.

현실적인 타협안으로 **대분류 2개(식료품 / 생활용품)로 먼저 나누는 방향**을 검토하고 있습니다.

```
식료품   → 원산지, 유통기한, 단위 중량당 가격, 브랜드 신뢰도 가중치 높임
생활용품 → 용량 대비 단가, 성분 동등성, 가격 변동성 가중치 높임
```

이렇게 하면 카테고리별 완전 분리보다 데이터 요구량을 대폭 줄이면서도
"기저귀와 쌀을 같은 기준으로 평가하는" 문제를 해결할 수 있습니다.
단, 대분류 내에서도 편차가 크기 때문에 이 역시 완전한 해결책은 아닙니다.
